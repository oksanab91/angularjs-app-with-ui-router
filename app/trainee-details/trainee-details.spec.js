'use strict';

describe('myApp.traineeDetails module', function() {

  beforeEach(module('myApp.traineeDetails'));

  describe('traineeDetails controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var traineeDetailsCtrl = $controller('TraineeDetailsCtrl');
      expect(traineeDetailsCtrl).toBeDefined();
    }));

  });
});