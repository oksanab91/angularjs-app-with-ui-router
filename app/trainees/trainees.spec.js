'use strict';

describe('myApp.trainees module', function() {

  beforeEach(module('myApp.trainees'));

  describe('trainees controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var traineesCtrl = $controller('TraineesCtrl');
      expect(traineesCtrl).toBeDefined();
    }));

  });
});